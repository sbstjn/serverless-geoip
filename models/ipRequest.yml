name: "ipRequest"
description: "a simple request model for ip/{ip}"
contentType: "application/json"
schema:
  type: object
  properties:
    text:
      type: string
      format: string
  required:
    - text